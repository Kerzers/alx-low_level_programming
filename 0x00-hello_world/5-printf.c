#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char a[56]= "with proper grammar, but the outcome is a piece of art,";
	
	printf("%s\n",a);
	return (0);
}
